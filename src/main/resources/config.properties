# Default application configuration properties

# Default locale settings
language=en
country=US

# API key (set via --set-apikey <apikey> or manually)
API_KEY=

# Initialization logging (true to show folder/resource init messages, false to hide)
config.verbose=false